{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "target": "ES5", // Target latest ECMAScript features for ES modules
    "module": "esnext", // Generate ES modules
    "outDir": "dist/esm" // Separate output directory for ES modules
  }
}